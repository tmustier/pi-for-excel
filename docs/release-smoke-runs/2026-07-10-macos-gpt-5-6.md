# 2026-07-10 macOS GPT-5.6 release smoke

## Build under test

- Commit: `6e1b0fc05e3a467403336b6b29d772ca91b6585f`
- Branch: `feat/gpt-5-6-models-release`
- Release target: `v0.10.0-pre`
- Pull request: [#644](https://github.com/tmustier/pi-for-excel/pull/644)

## Environment

- macOS 26.3.1 (25D2128), Apple silicon (`arm64`)
- Microsoft Excel for Mac 16.110.2
- Node.js 26.0.0
- Real sideloaded Excel taskpane served over local HTTPS
- Dev-only background verification bridge bound to loopback with a generated bearer token
- Foreground-application check: Superhuman remained frontmost before and after background Excel commands

## Evidence

- [Cropped real Excel taskpane status showing GPT-5.6 Sol and 372k context](https://github.com/user-attachments/assets/f8815a7c-ed73-41e5-af99-fcd6999fb9a7)
- [Thinking selector with separate Extra high and Max rows](https://github.com/user-attachments/assets/719abb48-1ab0-4e1d-8da0-99ccef1aae03)
- Canonical metadata and regression coverage: [`tests/model-ordering.test.ts`](../../tests/model-ordering.test.ts)
- Real-host selector command: [`selectModel` in the background verification bridge](../../src/taskpane/background-verification-bridge.ts)
- Automated run/check details: [PR #644 checks](https://github.com/tmustier/pi-for-excel/pull/644/checks)

## Results

`M-*` rows are release-specific checks supplementing the standard release checklist.

| ID | Area | Platform | Status | Evidence / notes |
|---|---|---|---|---|
| PRE-1 | Required command suite | macOS | Pass | `npm ci`; high-threshold audit; `check`; model/context/security/manifest tests; production build; Microsoft manifest validation. PR checks linked above. |
| M-1 | Exact GPT-5.6 discovery and selection | Excel for Mac | Pass | `openai-codex/gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna` were each found by exact provider/ID, clicked through the real picker, and became the active runtime model. Each row reported 372k context and 128k maximum output. |
| M-2 | Sol and Terra live completion | Excel for Mac | Pass | Both models returned assistant messages with `stopReason=stop` and the selected canonical model ID through `openai-codex-responses`. |
| M-3 | Luna availability boundary | Excel + native Pi | Upstream-limited | Luna selected and activated correctly in Excel. The ChatGPT SSE route used by the Excel WebView returned `Model not found gpt-5.6-luna`. Native Pi 0.80.6 Node `auto`/WebSocket completed the same canonical `openai-codex/gpt-5.6-luna` ID successfully; OpenAI API/OpenRouter routes also completed. |
| M-4 | Thinking controls | Browser taskpane | Pass | Registry-driven list rendered Off, Min, Low, Medium, High, Extra high, and Max as distinct choices. |
| M-5 | Background-host discipline | macOS | Pass | Taskpane commands were issued through the tokened loopback bridge; Excel was not activated and Superhuman remained frontmost. |

## Automated totals

- `npm run test:models`: 50 passed
- `npm run test:context`: 739 passed
- `npm run test:security`: 99 passed
- `npm run test:manifest`: 16 passed
- `npm run check`: passed, including Pi lockstep (`0.80.6`) and version synchronization (`0.10.0-pre`)
- `npm run build`: passed
- `npm run validate`: manifest valid
- `npm audit --audit-level=high`: passed; no high-severity findings (11 known moderate findings remain)

## Luna interpretation

The transport split was reproduced deliberately rather than inferred from the UI. The model ID, provider, metadata, credential mapping, and selector behavior are correct. The failing path is specifically the upstream ChatGPT SSE route; the Node WebSocket and API-provider paths accept Luna. Sol remains the default, so this rollout boundary does not put fresh sessions onto an unavailable tier.
